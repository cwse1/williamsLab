package components

func title(page string) string {
	if page == "Home" {
		return "Hematopathology"
	}
	return page
}

templ Hero(page string) {
	<div class="w-full relative text-center">
		<img src={""} class="order-last"/>
		<h1>{ title(page) }</h1>
	</div>
}
